---
title: Recipes
layout: 'layouts/default.njk'
---

<ul>
    {% for recipe in collections.recipes | sort(attribute='data.title') %}
    <li class="text-2xl flex gap-x-4 pb-4">
        <a class="underline underline-offset-4 decoration-2 decoration-red-600 hover:text-red-600" href="{{ recipe.url }}">{{ recipe.data.title }}</a>
        <span>#{{ recipe.data.category }}</span>
    </li>
    {% endfor %}
</ul>